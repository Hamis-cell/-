import telebot
import random
from bot_logic import gen_pass, gen_emodji, flip_coin 
from telebot.types import ReactionTypeEmoji

bot = telebot.TeleBot("")
    
@bot.message_handler(commands=['start'])
def send_welcome(message):
    bot.reply_to(message, "Привет! Я твой Telegram бот. Напиши команду /hello, /bye, /pass, /emodji или /coin  ")

@bot.message_handler(commands=['hello'])
def send_hello(message):
    bot.reply_to(message, "Привет! Как дела?")

@bot.message_handler(commands=['bye'])
def send_bye(message):
    bot.reply_to(message, "Пока! Удачи!")

@bot.message_handler(commands=['pass'])
def send_password(message):
    password = gen_pass(10)  
    bot.reply_to(message, f"Вот твой сгенерированный пароль: {password}")

@bot.message_handler(commands=['emodji'])
def send_emodji(message):
    emodji = gen_emodji()
    bot.reply_to(message, f"Вот эмоджи': {emodji}")
    

@bot.message_handler(commands=['coin'])
def send_coin(message):
    coin = flip_coin()
    bot.reply_to(message, f"Монетка выпала так: {coin}")

@bot.message_handler(commands=['echo'])
def send_coin(message):
    coin = flip_coin()
    bot.reply_to(message, f"вы можете заниматьсяя волонтёрством или перерабатывать мусор")





print ("бот работает")

@bot.message_handler(commands=['mem'])
def send_mem(message):
    with open('C:/Users/danii/OneDrive/Рабочий стол/bottt/images/mem1.jpg', 'rb') as f:  
        bot.send_photo(message.chat.id, f)  
@bot.message_handler(commands=['animals'])
def send_mem(message):
    with open('C:/Users/danii/OneDrive/Рабочий стол/bottt/images/animals1.png', 'rb') as f:  
        bot.send_photo(message.chat.id, f)  

bot.polling()

